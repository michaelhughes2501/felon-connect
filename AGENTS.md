# Repository Notes

## Current State
- This repository is initialized with Git but currently has no tracked application files or commits.
- No build, test, lint, or run commands have been discovered yet.

## Commands
- TODO: Add exact setup, build, lint, test, and run commands after the project scaffold is committed.

## Workflow Notes
- Use `cmd.exe` for local shell commands if the default PowerShell shim fails to launch in this workspace.
- Treat warnings about `C:\Users\MadMike\.config\git\ignore` as environment-level noise unless they start blocking Git operations.
